# Grafana (http://localhost:3000)
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme

# Watchtower — registry kontrol aralığı (saniye, varsayılan 300)
WATCHTOWER_POLL_INTERVAL=10

# PostgreSQL 17 — stage ve prod aynı container, farklı database adı (homiko-stage / homiko-prod)
POSTGRES_USER=homiko
POSTGRES_PASSWORD=changeme
POSTGRES_DB=homiko
# İsteğe bağlı: host’ta 5432 meşgulse örn. 5433
POSTGRES_PUBLISH_PORT=5432

# Private Docker Hub image ise watchtower servisine ekle:
# WATCHTOWER_REPO_USER=
# WATCHTOWER_REPO_PASS=
